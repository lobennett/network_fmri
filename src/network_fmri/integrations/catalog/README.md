# Built-in integration catalog

Add one versioned TOML manifest per external package. Manifests default to disabled;
operators activate them explicitly in pipeline configuration or with
`--enable-integration`. See `docs/EXTENDING.md` for the schema and contribution
checklist.
